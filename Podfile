
platform :ios, ‘7.0’

target :RNProject do

pod 'React', :path => ‘./ReactNative/node_modules/react-native', :subspecs => [
'Core',
'RCTNetwork',
'RCTText',
'RCTWebSocket',
# 添加其他你想在工程中使用的依赖。
]

end

